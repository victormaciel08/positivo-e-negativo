#include <stdio.h>
#include <stdlib.h>
#include <locale.h>

int main(void) {
		setlocale(LC_ALL, "Portuguese");
	char nome[30];
	
	printf("Digite seu nome e sobrenome: ");
//	scanf("%s", nome);
	scanf("%29[^\n]s", nome);
	printf("Seu nome é %s", nome); 
	return 0;
}
