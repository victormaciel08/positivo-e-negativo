#include <stdio.h>

int main(void) {

	int num = 0;

	printf("\nDigite um Numero para contagem regressiva: ");
	scanf(" %d", &num);
	
	while(num >= 0){
		printf("\n%d", num);
		num--;
	}
	return 0;	
}
